import Login from "@/components/login";

export const metadata = {
  title: "Login - AwasDhara Properties",
  description: "Login to your AwasDhara Properties account to access exclusive property listings and manage your preferences.",
};

export default function LoginPage() {
  return <Login />;
}